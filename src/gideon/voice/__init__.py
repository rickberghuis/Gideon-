"""Voice adapters (Tier 3): ears (STT) and mouth (TTS) around the same brain.

Voice changes how a turn arrives and leaves; it does not change what a turn is. These
modules import their heavy/optional dependencies lazily so the text harness runs without
audio libraries installed.
"""
